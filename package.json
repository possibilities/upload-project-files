{
  "name": "upload-project-files",
  "version": "0.0.2",
  "description": "a minimal api for uploading project files into clusterspace",
  "scripts": {
    "dev": "micro-dev",
    "start": "micro ./src/index.js",
    "lint": "standard",
    "test": "PROJECTS_PATH=/tmp/projects ava --verbose",
    "pretest": "standard"
  },
  "dependencies": {
    "fs-extra": "^7.0.1",
    "micro": "^9.3.3",
    "microrouter": "^3.1.3"
  },
  "devDependencies": {
    "ava": "^1.0.1",
    "axios": "^0.18.0",
    "micro-dev": "^3.0.0",
    "standard": "^12.0.1",
    "test-listen": "^1.1.0"
  }
}
